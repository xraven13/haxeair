package flash.security;

extern class RevocationCheckSettings {
	public static var ALWAYS_REQUIRED(default, null): String;
	public static var BEST_EFFORT(default, null): String;
	public static var NEVER(default, null): String;
	public static var REQUIRED_IF_AVAILABLE(default, null): String;
}
